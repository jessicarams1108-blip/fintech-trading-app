{
  "name": "fintech-trading-monorepo",
  "private": true,
  "workspaces": [
    "web",
    "server"
  ],
  "scripts": {
    "db:create": "npm run db:create -w server",
    "db:schema": "npm run db:schema -w server",
    "dev": "concurrently -k -n server,web -c blue,green \"npm run dev -w server\" \"node scripts/wait-and-dev-web.mjs\"",
    "dev:web": "npm run dev -w web",
    "dev:server": "npm run dev -w server",
    "build": "npm run build -w web && npm run build -w server",
    "build:vercel": "npm run build -w web",
    "start": "node server/scripts/ensure-db-ready.mjs && npm run start -w server",
    "preview:web": "npm run preview -w web"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "dotenv": "^16.5.0",
    "wait-on": "^8.0.5"
  }
}
