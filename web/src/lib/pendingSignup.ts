/** Keys used between signup register and /verify (align with product brief). */
export const PENDING_USER_ID_KEY = "pendingUserId";
export const PENDING_EMAIL_SESSION_KEY = "oove_pending_email";
/** Present only when the API has no Resend key (OTP is not emailed). */
export const PENDING_DEV_VERIFICATION_CODE_KEY = "oove_pending_dev_code";
